{
  "name": "@pairup/frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Frontend modules — built with esbuild, output consumed by @pairup/web. Empty placeholder; populated in PR 11 (AXI-120).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo \"frontend build lands in PR 11 (AXI-120)\" && exit 0"
  }
}
